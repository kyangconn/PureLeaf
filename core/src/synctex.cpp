// synctex.cpp — coordinate conversion is defined inline in the header.
// This translation unit exists so the library has a stable object for
// the symbol and to serve as the future home for reverse-search parsing
// (reading .synctex.gz files), which is not pure math.
//
// The forward conversion (synctexToPdfjs) is in pureleaf/synctex.h.
